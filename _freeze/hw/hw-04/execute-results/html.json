{
  "hash": "4e0df78b464851bfea5ef2ec04242f73",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"HW 04: Education & median income in US Counties\"\nauthor: \"Insert Name Here\"\nsubtitle: \"Residuals, R-squared, and RMSE\"\neditor: visual\nformat:\n  html:\n    embed-resources: true\n    include-in-header:\n      text: |\n        <style>\n        /* Exercise / answer boxes. Canonical source: style/exercise-boxes.css\n           in the course repo — copy from there if this ever drifts. */\n        body { counter-reset: exercise; }\n        .exercise, .answer {\n          border-left: 4px solid #533860;\n          padding: .6em 1em;\n          margin: 1em 0;\n          border-radius: 4px;\n        }\n        .exercise { background: #f2eef5; }\n        .exercise::before {\n          counter-increment: exercise;\n          content: \"Exercise \" counter(exercise);\n          display: block;\n          font-weight: 700;\n          color: #533860;\n          text-transform: uppercase;\n          letter-spacing: .04em;\n          font-size: .85em;\n          margin-bottom: .35em;\n        }\n        .answer {\n          background: transparent;\n          border-left-color: #a49363;\n          border-left-style: dashed;\n          min-height: 3.5em;\n        }\n        .answer::before {\n          content: \"Your answer\";\n          display: block;\n          font-weight: 700;\n          color: #8a7a4f;\n          text-transform: uppercase;\n          letter-spacing: .04em;\n          font-size: .85em;\n          margin-bottom: .35em;\n        }\n        .quarto-dark .exercise {\n          background: rgba(255, 255, 255, .08);\n          border-left-color: #FFCB70;\n        }\n        .quarto-dark .exercise::before {\n          color: #FFCB70;\n        }\n        .quarto-dark .answer::before {\n          color: #ffd283;\n        }\n        </style>\n---\n\n\n\n# Introduction\n\nIn this homework, you will use simple linear regression to examine the association between the percent of adults with a bachelor's degree and the median household income for counties in the United States. This builds directly on lessons 6 and 7: you'll fit a model, compute and interpret residuals, and use $R^2$ and RMSE to evaluate and compare models.\n\n## Learning goals\n\nBy the end of this homework, you will...\n\n- Fit and interpret a simple linear regression model\n- Compute and interpret residuals for individual observations\n- Decompose total variation into variation explained by the model and leftover variation ($SST = SSM + SSE$)\n- Compute and interpret $R^2$ and RMSE\n- Compare two models using $R^2$ and RMSE\n\n# Getting Started\n\n### Log in to RStudio\n\n- Go to [RStudio](https://rstudio.collegeofidaho.edu/) and log in with your College of Idaho Email and Password.\n\n- Use the `hw` folder you made earlier in the semester. Create a subfolder called `hw04` to store this homework, and a `data` subfolder inside of it.\n\n- Log in to [Canvas](https://cofi.instructure.com/courses/18743/assignments/243850), navigate to HW-04, and download the `hw-04.qmd` and `us-counties-sample.csv` files. Upload the `.qmd` into the folder you just made, and the `.csv` into the `data` subfolder.\n\n## R and RStudio\n\nSee the [R resources](https://ericfriedlander.github.io/math2025-fa26/computing-r-resources.html) page for a guide to the RStudio IDE and Quarto file components.\n\n# Packages\n\nWe will use some of the following packages in this HW. I'd like for you to practice only using the packages you need so I've commented out all of the packages. As you work through the activities, I'd like you to focus on uncommenting packages as you need them. For example, in the first exercise you'll need to use the `read_csv` and `glimpse` functions. These both come from the `tidyverse` package so you'd uncomment and load that package before trying to run those functions. If you can't figure out which package a certain function is from here are some tips:\n\n1. Start googling it! This may not always work because some of our packages are what are called \"meta-packages\" and contain many other packages. For example, `read_csv` and `glimpse` are actually from packages called `readr` and `dplyr`, respectively, but are part of the `tidyverse` and loaded automatically when you load the `tidyverse` package.\n2. It helps to know what the individual packages are for:\n    - `tidyverse` will be in charge of most of your data manipulation so anything that involves loading or cleaning data will typically rely on `tidyverse`\n    - `ggformula` is what makes our plotting work so anything that starts with `gf_` will need `ggformula`\n    - `broom` is for cleaning models so anything like `tidy` or `augment` will come from `broom`\n    - `yardstick` is for evaluating models so you'll want to use this for anything that computes a model evaluation metric (e.g. RMSE or $R^2$)\n    - `supernova` is for the `supernova` function\n    - `knitr` will provide you with the `kable` function if you choose to use it\n\nIf you still can't figure it out, feel free to uncomment ALL of the packages, but I recommend coming to see Dr. F for help. \n\n\n::: {.cell}\n\n```{.r .cell-code}\n# library(tidyverse)\n# library(ggformula)\n# library(broom)\n# library(yardstick)\n# library(supernova)\n# library(knitr)\n```\n:::\n\n\n# Data: US Counties\n\nThe data are from the `county_2019` data frame in the [usdata](https://openintrostat.github.io/usdata/index.html) R package. These data were originally collected in the 2019 [American Community Survey](https://www.census.gov/programs-surveys/acs) (ACS), an annual survey conducted by the United States Census Bureau that collects demographics and other information from a sample of households in the United States. The data in `county_2019` are county-level statistics from the ACS.\n\nThe primary variables you will use are the following:\n\n- `bachelors`: Percent of population 25 years old and older that earned a Bachelor's degree or higher\n- `median_household_income`: Median household income in US dollars\n- `household_has_computer`: Percent of households that have a desktop or laptop computer\n\n[Click here](https://openintrostat.github.io/usdata/reference/county_2019.html) for the full codebook for the `county_2019` data set.\n\n# Exercises\n\nThere has been a lot of conversation recently about the impact of graduating college, i.e., obtaining a bachelor's degree, on one's future career and lifetime earnings. The common sentiment is that individuals who have earned a bachelor's degree (or higher) will earn more income over the course of a lifetime than an individual who does not have such a degree.\n\nThe culmination of individual factors can impact the wealth and resources of a county. Therefore we will examine the association between these two factors at a county level, using the percent of adults 25 years old and older with a Bachelor's degree to understand variability in median income.\n\n::: callout-important\nAll narrative should be written in complete sentences, and all visualizations should have informative titles and axis labels.\n:::\n\n::: callout-tip\nWrite all code and narrative in your Quarto file. Throughout the assignment, you should periodically **render** your Quarto document to produce the updated HTML file.\n\nMake sure we can read all of your code in your HTML file. This means you will need to break up long lines of code. One way to help avoid long lines of code is to start a new line after every pipe (`|>`).\n\nIn your `hw-04.qmd` document you'll see that each task is wrapped in an `exercise` box, with an empty `answer` box below it for you to fill in your response. Use this format for the remaining exercises.\n:::\n\n## Part 1: Exploratory data analysis\n\n::: exercise\nLoad `us-counties-sample.csv` into a data frame called `county_data`. \n\nHow many counties are in the data set?\n:::\n\n::: answer\n\n::: {.cell}\n\n:::\n\n:::\n\n::: exercise\nCreate a visualization of the distribution of `bachelors` and calculate appropriate summary statistics. Use the visualization and summary statistics to describe the distribution. Include an informative title and axis labels on the plot.\n:::\n\n::: answer\n\n::: {.cell}\n\n:::\n\n:::\n\n::: exercise\nCreate a visualization of the relationship between `bachelors` and `median_household_income`, and calculate the correlation. Note that there are several ways to do this. I recommend piping your dataset into `summarize` and then using `cor`. You may use AI to help with this problem without citation. \n\nUse the visualization and correlation to describe the relationship between the two variables.\n:::\n\n::: answer\n\n::: {.cell}\n\n:::\n\n:::\n\n::: callout-tip\nThis is a good place to render your document.\n:::\n\n## Part 2: Fitting the model\n\n### A Brief Introduction to LaTeX\n\nWhen typesetting equations, mathematicians, statisticians, and data scientists often use something called LaTeX (pronounced \"Lah-Tech\" or \"Lay-Tech\").\nQuarto makes it easy to incorporate LaTeX equations into your prose.\nTo enter an equation you do so in \"Math Mode\".\nAll this means is surrounding your equation by dollar signs.\nUse one dollar sign on each side like this (don't worry, you're not supposed to understand what the stuff inside the dollar signs is doing), $\\bar{x} = \\frac{\\sum_{i=1}^n x_i}{n}$ if you want the equation to remain in line.\nUse two dollar signs on each side like this, $$\\bar{x} = \\frac{\\sum_{i=1}^n x_i}{n}$$ if you want the equation to be displayed on its own line.\nNote that you'll have to render the document to see the difference.\nWithin math mode you can easily create a variety of special symbols, each of which starts with a backslash, like Greek letters (e.g. $\\beta$, $\\epsilon$, $\\alpha$).\nYou can also add subscripts using an underscore (e.g. $\\alpha_{3}$, $x_{i}$) and superscripts using a caret (e.g. $x^{2}$, $\\epsilon^\\alpha$), just make sure to surround the sub- or superscript with curly braces.\nSub- and superscripts will still work without the curly braces if there's only one digit, but you need the braces for more than one digit (e.g. render to see the difference between $x^21$ and $x^{21}$).\nOne more thing to note.\nIf you try to write normal text in Math Mode, LaTeX will think that each letter is a variable and display it in italics and remove spaces $like this$. \nWhen it comes to making a nice, presentable document, it's best to have words drafted as if they were text.\nThe way to do this in Math Mode is to use the `\\text{}` command.\nSimply input the word you want rendered as text in the curly braces and it will appear normally (e.g. $\\text{normal text}$ versus $not normal text$). \nThis will come in handy when you want to write the name of variables inside an equation.\n[Click here](https://oeis.org/wiki/List_of_LaTeX_mathematical_symbols) for a guide on writing mathematical symbols using LaTeX. \n\n:::{.callout-warning}\nPro-tip: Be careful when googling \"Latex tutorials\" as you may get some adult content that you weren't asking for.\n:::\n\n:::: exercise\nUse Math Mode to write the form of the statistical model in mathematical notation (i.e., with Greek letters) and variable names (`bachelors` and `median_household_income`) in the equation. I'm not asking you to fit the model yet: just write the general form of the model, using the names of the variables and Greek letters.\n\n1. Start by going into the answer block below and creating Math Mode by typing either two or four dollar signs (one or two on each side). I recommend using four (two on each side) so that it centers the equation nicely in your final document.\n2. In between the dollar signs, write out the equation. You'll want to use the following symbols:\n    - `\\beta`: you'll need this twice and you'll need to add the appropriate subscripts using underscores\n    - `\\epsilon`\n    - `\\text{variable\\_name}`: where you replace `variable\\_name` with the actual name of the variable. One more thing to note. Since the underscore typically tells LaTeX to make a subscript, you need to put a backslash in front of any underscores that you want to appear as underscores so instead of using `\\text{median_household_income}` you'll want to use `\\text{median\\_household\\_income}`. This is called \"escaping\".\n    - `\\times`: this will create a multiplication symbol\n    - `=` and `+` will appear normally\n::::\n\n::: answer\n:::\n\n::: exercise\nFit the regression line corresponding to the statistical model in the previous exercise, and call it `bachelors_mod`. Use `tidy()` to neatly display the model.\n\nUnderneath the output of the model, use Math Mode to write the equation of the fitted model using mathematical notation. This should be easier because you shouldn't have to write any Greek letters. However, make sure you use the variable names again (`bachelors` and `median_household_income`) in the equation. Recall that for the regression equation you'll need to use a \"hat\". You can put a hat over something by using the `\\hat{thing to put the hat over}` LaTeX command. For example $\\hat{\\text{I\\_Have\\_A\\_Hat}}$ and $\\hat{\\beta_0}$.\n:::\n\n::: answer\n\n::: {.cell}\n\n:::\n\n\nPut your equation here.\n:::\n\n::: exercise\n- Interpret the slope of `bachelors_mod`. The interpretation should be written in a way that is meaningful in the context of the data.\n- Is it useful to interpret the intercept for this data? If so, write the interpretation in the context of the data. Otherwise, briefly explain why not.\n:::\n\n::: answer\n:::\n\n::: callout-tip\nThis is a good place to render your document and to make sure all of your equations appear like you want them to!\n:::\n\n## Part 3: Residuals\n\n::: exercise\nChoose one county from `county_data` (use `filter()` with its `name`) and record its actual `bachelors` and `median_household_income` values.\n\n- Using the fitted equation from Exercise 5, compute this county's **predicted** median household income using only addition, multiplication, subtraction, and division (i.e., by plugging into the equation, not with the `predict` or `augment` functions).\n- Similarly, compute the **residual** for this county without using any functions.\n- Is this county above or below the regression line? Did the model over- or underpredict this county's income? How can you tell?\n:::\n\n::: answer\n\n::: {.cell}\n\n:::\n\n:::\n\n::: exercise\nUse `augment()` on `bachelors_mod` to get fitted values and residuals for every county, and save the result to `bachelors_aug`.\n\n- Use the verb `arrange()` from `dplyr` to find the county with the **largest positive** residual and the county with the **largest negative** residual. Hint: pipe your augmented dataset into `arrange` and use the name of the variable you want to sort by as the argument. This will sort it in increasing order and the minimum value will appear at the top. To sort in descending order, use `arrange(desc(var_name))` instead of `arrange(var_name)`.\n- Interpret each of these two residuals in the context of the data (what does it mean for that county's actual vs. predicted income?).\n:::\n\n::: answer\n\n::: {.cell}\n\n:::\n\n:::\n\n::: callout-tip\nThis is a good place to render your document.\n:::\n\n## Part 4: Variation, $R^2$, and RMSE\n\n::: exercise\nWe'll now compute SST and SSE of `bachelors_mod` the \"hard way\" and then verify our results using `supernova`:\n\n1. Fit the **empty model**, `empty_mod <- lm(median_household_income ~ NULL, data = county_data)`. What value does this predict for every observation (looking for the name, not the actual number)?\n2. Use `augment` to get the residuals of the empty model and then use `mutate` to square them, and `summarize` to sum up the squared residuals.\n3. Repeat step 2 with `bachelors_mod`.\n4. Which of these two numbers is SSE and which is SST?\n5. Use `supernova` on `bachelors_mod` to verify the results of your SSE and SST. Why don't we need to use it for the empty model?\n6. Use these numbers to compute the SSM.\n:::\n\n::: answer\n\n::: {.cell}\n\n:::\n\n:::\n\n::: exercise\nUse the `rsq()` and `rmse()` functions from `yardstick` on `bachelors_aug` to compute $R^2$ and RMSE for `bachelors_mod`.\n\n- In addition, use the results of the previous exercise to compute $R^2$ and verify that the two values are equal.\n- Write one sentence interpreting $R^2$ and one sentence interpreting RMSE, in the context of the data.\n:::\n\n::: answer\n\n::: {.cell}\n\n:::\n\n:::\n\n::: exercise\nFit a second model, `computer_mod`, predicting `median_household_income` from `household_has_computer`. Use `augment()`, then `rsq()` and `rmse()` to evaluate it.\n\nFill in the table below comparing the two models.\n\n| Model          | Predictor                | $R^2$ | RMSE |\n|:---------------|:-------------------------|:-----:|:----:|\n| `bachelors_mod`   | `bachelors`              |       |      |\n| `computer_mod` | `household_has_computer` |       |      |\n\nWhich model does a better job predicting median household income? Explain how you decided, using both $R^2$ and RMSE.\n:::\n\n::: answer\n\n::: {.cell}\n\n:::\n\n:::\n\n::: callout-tip\nThis is a good place to render your document.\n:::\n\n## Conceptual Questions\n\nDo not write any code to answer these. These are meant to test your conceptual understanding.\n\n::: exercise\nSuppose you created a new column in `county_data` called `income_thousands`, equal to `median_household_income` divided by 1000 and then refit `bachelors_mod` using `income_thousands` as the response instead. \n\nHow would the $R^2$ and RMSE of this model compare to our original `bachelors_mod`? Namely, would these numbers increase, decrease, or remain the same? Explain why. Hint 1: $R^2$ and RMSE both have answers (although different from one another) and the answer isn't any variation of \"It depends\" or \"We need more information\". Hint 2: Thinking about the units may be helpful.\n:::\n\n::: answer\n:::\n\n::: exercise\nSuppose you were to convert `bachelors` into a `factor` and use it as the explanatory variable (instead of a number) to predict `median_household_income`. Although it is not true for this data, assume that every county has a unique value of `bachelors`. That is, assume that no two counties have the exact same value in the `bachelors` column. What would you expect the $R^2$ and RMSE of this model to be? Explain why. Based on your answer, describe whether you think it would be a good idea to use this model.\n:::\n\n::: answer\n:::\n\n::: callout-tip\nYou're done and ready to submit your work! Render your document and upload the .qmd and .html files to Canvas.\n:::\n\n# Grading (30 pts)\n\n<br>\n\n| Component             | Points |\n|:----------------------|:-------|\n| Ex 1                  | 1      |\n| Ex 2                  | 2      |\n| Ex 3                  | 2      |\n| Ex 4                  | 2      |\n| Ex 5                  | 2      |\n| Ex 6                  | 2      |\n| Ex 7                  | 2      |\n| Ex 8                  | 2      |\n| Ex 9                  | 4      |\n| Ex 10                 | 3      |\n| Ex 11                 | 2      |\n| Ex 12                 | 2      |\n| Ex 13                 | 2      |\n| Grammar & Writing     | 1[^1]  |\n| Workflow & formatting | 1[^2]  |\n\n[^1]: The \"Grammar & Writing\" grade is decided based on your grammar and writing. This is typically decided by choosing one of the questions and assessing the writing.\n\n[^2]: The \"Workflow & formatting\" grade is to assess the reproducible workflow and document format. This includes having a neatly organized document with readable code and your name and the date in the YAML.",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}